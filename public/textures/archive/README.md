# Archived Background Textures

These SVG turbulence textures are kept as dormant design references from the
earlier background pass.

They are not referenced by the site, so they do not affect page load. To try
one again, use it as a CSS `background-image` from `/textures/archive/...`.
